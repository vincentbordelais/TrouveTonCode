{% extends "layout.html.twig" %}

{% block body %}
    <h1>Liste des utilisateurs</h1>

    <ul>
        {% for user in users %}
            <li>{{ user.username }}</li>
        {% endfor %}
    </ul>
{% endblock %}